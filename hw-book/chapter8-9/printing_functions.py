#from hw_function import build_car as funct_build

#    funct_build.user_car = build_car('land rover', 'defender', color='orange', two_package=True)
#    funct_build.new_car = build_car('porshe', '911', color='blue')

#print(user_car)
#print(new_car)


import hw_function

    user_car = hw_function.build_car('land rover', 'defender', color='orange', two_package=True)
    new_car = hw_function.build_car('porshe', '911', color='blue')

print(user_car)
print(new_car)